# .env.example

# Supabase public (browser) config
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Supabase service role key (server-only) - never commit the real key
SUPABASE_SERVICE_ROLE_KEY=
